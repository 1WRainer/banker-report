name: Daily BankerReport Fetcher

on:
  schedule:
    # 12:00 UTC corresponds to 7:00 AM CST
    - cron: '0 12 * * *'
  workflow_dispatch: # Allows triggering the script manually for testing

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          pip install feedparser

      - name: Run News Scraper
        run: |
          python fetch_news.py

      - name: Commit and Push Updated News
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add data.json
          git commit -m "Auto-update daily news for BankerReport.com [7:00 AM CST]" || exit 0
          git push
